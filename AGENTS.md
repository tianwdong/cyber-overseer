# Cyber Overseer

- 核心目标是默认自动发现全部已打开的本机 Codex 根任务，并恢复网络和压缩故障；动画不应成为恢复前置条件。
- 完整任务 ID 是身份依据，不用标题、模糊文字或全局键盘焦点定位发送目标。
- Codex 数据库和 rollout 只读。保留原任务模型、权限、工作目录及审批设置。
- 日志未证明失败时，不把静默、正常完成、人工停止或等待批准当作故障。
- 内部 IPC 属实验性适配器；修改方法、版本或日志字段须核对实际安装版本及源证据。
- 修改恢复逻辑后运行 `npm run typecheck && npm test`；UI 改动运行 `npm run smoke` 并查看截图。
- 持续区分模拟协议测试、真实接口探测、真实故障恢复和真实窗口定位的验收证据。
- 不覆盖工具对 Codex UI 访问的限制，不通过替代手段绕过拒绝。
